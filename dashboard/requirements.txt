# Serving layer (Streamlit dashboard reading Athena). Install:  pip install -r dashboard/requirements.txt
streamlit>=1.36
awswrangler>=3.9      # one-liner Athena -> pandas (pulls boto3 + pyarrow)
pandas>=2.2
plotly>=5.22
